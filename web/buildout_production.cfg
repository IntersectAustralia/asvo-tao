[buildout]
extends = buildout.cfg

[sources]
light-cone = fs light-cone full-path=${buildout:directory}/src/light-cone
sed = fs sed full-path=${buildout:directory}/src/sed

[django]
settings = production
extra-paths = /home/taoadmin/etc/tao/

